require 'spec_helper'

describe Opal::Aasm do
  it 'has a version number' do
    expect(Opal::Aasm::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
